import { CompatibilityDirective } from './compatibility.directive';

describe('CompatibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new CompatibilityDirective();
    expect(directive).toBeTruthy();
  });
});
