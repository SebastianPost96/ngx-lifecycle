import { AfterViewInitDirective } from './after-view-init.directive';

describe('AfterViewInitDirective', () => {
  it('should create an instance', () => {
    const directive = new AfterViewInitDirective();
    expect(directive).toBeTruthy();
  });
});
